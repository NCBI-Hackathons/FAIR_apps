#
#   CIAO Application Makefile
#


# location of CIAO Master Makefile
MASTER_MAKEFILE ?= /usr/local/share/ciao/CIAO.Master.Makefile

# include CIAO Master Makefile,
# contains targets for make image / make deploy / make release
include $(MASTER_MAKEFILE)

# target to build/fetch any assets required by the docker image
# this gets called just prior to 'make image'
assets: 
	@echo "### Build Assets (NoOp)"


